(ns info.kovanovic.camelclojure.message-transformation.sort
  (:use [clojure.test]
        [info.kovanovic.camelclojure.dsl]
	[info.kovanovic.camelclojure.test-util]))

(deftest sort-pattern
  (is true))
