(ns info.kovanovic.camelclojure.message-transformation.validate
  (:use [clojure.test]
        [info.kovanovic.camelclojure.dsl]
	[info.kovanovic.camelclojure.test-util]))

(deftest validate-pattern
  (is true))
