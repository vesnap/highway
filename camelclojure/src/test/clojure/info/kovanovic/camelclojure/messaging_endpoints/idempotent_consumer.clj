(ns info.kovanovic.camelclojure.messaging-endpoints.idempotent-consumer
  (:use [clojure.test]
        [info.kovanovic.camelclojure.dsl]
	[info.kovanovic.camelclojure.test-util]))

(deftest idempotent-consumer-pattern
  (is true))
