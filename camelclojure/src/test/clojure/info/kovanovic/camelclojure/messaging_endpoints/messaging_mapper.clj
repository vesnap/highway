(ns info.kovanovic.camelclojure.messaging-endpoints.messaging-mapper
  (:use [clojure.test]
        [info.kovanovic.camelclojure.dsl]
	[info.kovanovic.camelclojure.test-util]))

(deftest messaging-mapper-pattern
  (is true))
