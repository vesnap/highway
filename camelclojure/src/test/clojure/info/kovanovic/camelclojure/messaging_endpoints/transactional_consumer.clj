(ns info.kovanovic.camelclojure.messaging-endpoints.transactional-consumer
  (:use [clojure.test]
        [info.kovanovic.camelclojure.dsl]
	[info.kovanovic.camelclojure.test-util]))

(deftest transactional-consumer-pattern
  (is true))
